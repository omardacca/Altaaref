<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Altaaref.Views.NotebooksDB.NotebookDetails">
    <Grid>
        <ActivityIndicator IsRunning="{Binding Busy}"
                           IsVisible="{Binding Busy}"
                           Color="Black"
                           VerticalOptions="CenterAndExpand"
                           HorizontalOptions="CenterAndExpand"/>

        <StackLayout Padding="5">
            <Label FontSize="Large" Text="{Binding Notebook.Name}" />

            <Label Text="{Binding Notebook.Views, StringFormat='{0} views'}"/>
            <Label Text="{Binding Notebook.CreationDate, StringFormat='{0:D}'}"/>

            <StackLayout VerticalOptions="EndAndExpand">
                <Button Text="Download" BackgroundColor="DarkRed" TextColor="White" Clicked="OnDownloadButtonClicked" />
                <Button Text="Add to Favorites" BackgroundColor="WhiteSmoke" TextColor="DarkSlateBlue" />
            </StackLayout>
        </StackLayout>

    </Grid>
</ContentPage>